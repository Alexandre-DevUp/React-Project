// Faz com que ao clicar no LOGO da página, o usuário seja redirecionado para a página inicial

// ASSETS
import { Children } from 'react';
import './Button.css';

function Button {{ arrow, buttonStyle, loading, Children, }}

export default Button;